# Origin `.opj` figures (graph windows) — reverse-engineering notes

**Consolidated into [`../origin_project_format.md`](../origin_project_format.md)
on 2026-07-04** (plan item 20) — see §6.1 "`.opj` figures — Graph → Layer →
Curve" for the object model, axis-range grammar, axis titles/legend/escape
syntax, the DataPlot curve record, and §6.3 for the proposed Origin →
quantized figure mapping and the permanent gap list (curve→column binding,
multi-layer layout, etc.).

The original reverse-engineering narrative (investigation process, hex
evidence, and confidence notes as first written) is preserved in this file's
git history — see `git log --follow -- docs/origin_re/opj_figures.md`.
